# Study Planner Project

## Setup Instructions

### 1. Install MySQL and Create Database
- Open MySQL and run the `database_setup.sql` script.

### 2. Configure Database Connection
- Open `database.py` and update the `host`, `user`, and `password`.

### 3. Install Required Libraries
Run the following command to install dependencies:
```
pip install mysql-connector-python tkinter
```

### 4. Run the Application
Run the Tkinter GUI using:
```
python study_planner.py
```

Enjoy your Study Planner! 📚
